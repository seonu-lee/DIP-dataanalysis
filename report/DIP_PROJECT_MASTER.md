# DIP 프로젝트 마스터 문서

> **상황 인식 LLM 음악 추천 서비스 (DIP)** — 데이터 분석가(DA) 관점 프로젝트 아카이브
>
> 본 문서는 포트폴리오 · 자기소개서 · 면접 답변 · 기술 블로그 · 경력기술서 · 회고 작성 시 참조할 **단일 원본(Single Source of Truth)** 으로 작성되었다. 결과 수치보다 **과정과 의사결정**에 무게를 두었으며, 검증되지 않은 항목은 `확인 필요`로 표시했다. 채용 담당자 관점에서 가치가 높은 지점은 `💡 어필 포인트`로 별도 표시했다.

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| **프로젝트명** | DIP — LLM 기반 상황 인식 음악 추천 서비스 (내부 엔진명: `soundscape-music-agent`) |
| **진행 주체** | TAVE 연합 동아리 |
| **진행 기간** | 사전배포 로그 기준 2026-01-01 ~ 2026-01-17 (전체 프로젝트 기간은 `확인 필요`) |
| **팀 구성** | 백엔드, 프론트엔드, 디자인, PM, **DA(선우, 팀 내 유일한 데이터 분석가)** |
| **본인 역할** | 분석 방향·범위·방법론 전체 정의, 데이터 수집/전처리, 추천 로직 근거 설계, KPI 설계·측정, 사전배포 제안 및 로그 스키마 설계, 사용자 로그 분석, 추천엔진(LangGraph) 설계 참여 |

### 프로젝트 배경
기존 음악 추천 앱은 장르나 기분을 직접 고르는 방식이다. DIP는 **"지금 카페에서 공부 중이고 소음이 꽤 큰 상황"** 같은 복합적 맥락을 LLM이 해석해 플레이리스트를 추천한다는 아이디어에서 출발했다. 즉, 사용자의 현재 **위치 · 활동 목표 · 주변 소음(dB)** 을 입력받아 상황에 맞는 음악을 골라준다.

### 프로젝트 목표
- 모델을 직접 학습시키기보다 **LLM의 컨텍스트 구성과 프롬프트 설계**로 상황 인식 추천을 구현한다.
- 추천 로직의 임계값(어떤 소음·목표에 어떤 음악 특성이 적합한가)을 **데이터 기반으로** 도출한다.
- 추천 품질을 측정할 수 있는 **자체 KPI 체계**를 설계하고 실사용자 로그로 검증한다.

### 해결하고자 한 비즈니스 문제
"LLM에게 추천을 시키면 결과는 나온다. 하지만 ① 그 추천이 정말 이 상황에 맞는지, ② 어떤 기준으로 판단해야 하는지, ③ 시스템이 얼마나 잘 작동하는지는 데이터 없이 답할 수 없다." → DA로서 이 세 질문을 풀기 위해 분석을 설계했다.

---

## 2. 프로젝트 전체 타임라인

```
[문제 정의]
  └ LLM 추천의 '정확함'을 무엇으로 정의할 것인가 → 정답 레이블/ground truth 부재 인식
       ↓
[데이터 수집·전처리]
  └ 외부 데이터셋 3종 수집 → 7개 장소 카테고리 매핑, 5개 소음 구간 정의
       ↓
[가설·추천 로직 설계]
  └ 3개 데이터셋 연결 → 소음→목표→위치 우선순위, 상황별 추천 매트릭스 도출
       ↓
[추천엔진 개발(반복)]
  └ Rule-based v1.0 → 단순 GPT(할루시네이션) → LangGraph+Spotify API v2.0
       ↓
[KPI 설계]
  └ 8개 설계 → API 제약/DB 설계/우선순위로 3개 탈락 → 최종 5개 측정
       ↓
[사전배포 제안·실행]  ← DA가 직접 제안 (팀 원안에 없던 단계)
  └ 로그 스키마 직접 정의 → 18명, 약 300건 로그 수집
       ↓
[사용자 로그 분석]
  └ 경향성 / 만족도 / 세그먼트 / 시계열 / 최신성 / 전환율 분석
       ↓
[인사이트 도출 → 팀 의사결정 반영]
  └ 이탈 원인 분석 → 개인화 도입 시 응답속도 모니터링 강화로 우선순위 변경
       ↓
[발표·회고]
```

`💡 어필 포인트` — 타임라인의 핵심은 **분석가가 "분석할 환경(사전배포·로그 스키마)"을 직접 만든 뒤 분석에 들어갔다**는 점이다. 주어진 데이터를 받아 분석한 것이 아니라, 측정 가능한 상태를 설계부터 만들었다.

---

## 3. 데이터 이해

### 3-1. 사용 데이터 (외부 3종)

| 데이터셋 | 출처 | 규모 | 주요 변수 | 활용 목적 |
|---|---|---|---|---|
| 한국환경공단 환경소음 측정 데이터 | 공공데이터포털 (data.go.kr), 2024년 | `확인 필요`(행 수) | 위치별 소음도(dBA), 측정지점 좌표/주소, 용도구분 | 장소 유형별 **표준 소음 프로필** 구축 |
| RAED (RealLife Activity & Environment Dataset) | Kaggle | 30,000행 | 행동, 위치(14종), 소음(dB), 조도, 활동량, 온습도 | 행동×환경 조합의 **실제 수치** 확인 |
| Spotify Music Dataset (High & Low Popularity) | Kaggle (Spotify API 수집) | `확인 필요`(행 수) | energy, danceability, valence, tempo, loudness, acousticness, instrumentalness, speechiness, 인기도, 장르 | 장르별 **오디오 특성 프로파일** 도출 |

> 환경공단 데이터는 3개 파일(환경소음 측정망 정보(수동측정, 분기별) / 소음진동측정망 운영정보(자동측정, 일별) / 측정망 지점 정보(위경도))을 **측정지점명 기준 병합**해 구성.

### 3-2. 사전배포 수집 데이터 (실사용자 로그 3종)
사전배포 기간 중 앱 서버 DB에서 주기적으로 export한 배치 데이터.

| 파일 | 내용 | 핵심 컬럼 |
|---|---|---|
| `playlists_final.xlsx` | 전체 추천 세션 로그 (매 추천 요청 기록) | id(PK), user_id, location, goal, decibel, recommendation_reason, user_persona(preferred_genres/taste_summary), search_query, latency_ms, inference_count, tool_call_success, deeplink, retry, created_at |
| `spotify_link_click.xlsx` | "Spotify에서 듣기" 딥링크 클릭 로그 (클릭 시에만 행 생성) | id, playlist_id(FK), user_id, minutes_from_creation, is_retry_clicked, clicked_at |
| `reviews.xlsx` | 사용자 피드백 (팝업) | id, user_id, rating(0~4), will_reuse(T/F), dislike_reason, lyrics_preference(on/off), preferred_mood(energetic/calm), feedback(주관식), created_at |

### 3-3. 입력값 정의
- **decibel**: quiet / moderate / loud
- **goal**: 활력 / 안정 / 집중 / 수면 / 분노 / 위로 / 휴식 / 미설정 (8종)
- **location**: 도서관 / 집·실내 / 이동중 / 카페 / 헬스장 / 코워킹 / 공원 (7종)
- **장르**: 팝, 케이팝, 밴드, 힙합, 발라드, 인디, 재즈, 클래식, 락, EDM, OST, R&B

### 3-4. 데이터 특징 및 품질 이슈
- **장소보다 행동이 소음을 더 강하게 결정**: 침실에서 stressed(86dB)가 도서관에서 studying(44dB)보다 시끄러움 → 장소 정보만으로 추천하면 안 되고 실시간 소음 측정이 필수라는 근거.
- **소음 분포 편중**: 측정값 대부분이 51~80 dBA에 집중 → 이를 반영해 구간 경계 설정.
- **표본 규모 한계**: 사전배포 18명 / 약 300건. 통계적 유의성 확보엔 부족(예: 이탈 2명).
- **DB 설계 이슈**: retry로 생성된 세션이 초기 추천 세션과 동일 형태로 `playlists_final`에 기록 → retry 세션 구분 불가 → Re-roll Rate 측정 실패.
- **로그 누락**: DLC 클릭 로그가 사전배포 1주차에 미기록 → 초기 전환율 일부 손실(2주차부터 기록).
- **매핑 누락**: 환경공단 매핑 함수에서 `Crowded Place`에 대한 명시 처리가 없어 기본값(co-working)으로 fallback.

---

## 4. 분석 과정 상세 정리 (무엇을 / 왜 / 대안 / 선택 이유 / 결과)

### 4-1. 장소·소음 표준 프로필 만들기 (환경공단 데이터)
- **무엇을**: 14종 측정 환경을 서비스 7개 장소로 매핑, 소음 5구간 정의, 장소별 평균·중앙값·표준편차 산출.
- **왜**: 사용자가 "카페에 있어요"라고 입력하면 카페가 보통 얼마나 시끄러운지를 LLM 컨텍스트로 넘겨야 추천 정확도가 오른다.
- **대안**: 단순 평균 dBA만 사용 vs 평균+변동성 함께 사용.
- **선택 이유**: 같은 평균이라도 지속 소음(백색소음형, moving)과 이벤트성 소음(변동형, home)은 추천 전략이 다르므로 **변동성(표준편차)** 까지 봤다.
- **결과**: 장소별 평균 dBA — home(53.4) < library(57.4) ≈ co-working(57.5) < cafe(59.5) < moving(67.0). moving은 평균 높고 std 낮은 안정적 고소음, home은 평균 낮지만 std 큰 변동 환경.

**소음 구간(Noise Band) 정의**

| 구간 | 범위(dBA) | 대표 환경 |
|---|---|---|
| Very Quiet | 0–35 | 심야 침실 |
| Quiet | 36–50 | 도서관 |
| Moderate | 51–65 | 일반 카페 |
| Loud | 66–80 | 이동 중 |
| Very Loud | 81+ | 매우 혼잡한 공간 |

### 4-2. 행동×환경 실제 수치 확인 (RAED)
- **무엇을**: 14개 위치를 7개로 통합(소음 분포 유사성 + 추천 전략 동일성 두 기준 적용), 5개 행동별 환경 변수 분포 분석.
- **왜**: 같은 카페라도 공부할 때(44.8dB)와 식사할 때(67.6dB)의 체감 소음이 약 23dB 차이.
- **결과(행동별 소음/활동)**:
  - sleeping 27.7dB / 1.5
  - studying 44.7dB / 2.5
  - eating 67.6dB / 4.0
  - walking 77.8dB / 8.0
  - stressed 87.5dB / 6.5
- **인사이트**: "카페 = 집중 음악"이라는 단순 공식 대신 **"카페+40dB대=집중용, 카페+60dB대=일상 팝"** 으로 세분화.

### 4-3. 장르별 음악 특성 기준 도출 (Spotify)
- **무엇을**: 7~8개 핵심 오디오 특성 선별, 장르별 평균 프로파일 생성.
- **결과 예시**:
  - studying(저소음·저활동) → classical(energy 0.20, instrumentalness 0.73), lofi(energy 0.29, instrumentalness 0.83)
  - walking/active(고소음·고활동) → pop/hip-hop(energy 0.64+, tempo 120~130 BPM)
  - stressed → **아이소 원리(Iso-principle)**: 현재 감정 에너지와 비슷한 곡(metal/rock)에서 시작해 점차 밝은 분위기(reggae valence 0.75, disco 0.70)로 전환
  - wellness → 수면 압도적 1순위(energy 0.007, loudness −38dB, acousticness 0.95)

### 4-4. 3개 데이터셋 연결 (핵심 차별화)
세 데이터셋은 독립 분석 시 각각의 인사이트만 주지만, **연결하면 추천 로직의 임계값**이 나온다.

```
[사용자 입력]
   ↓ 환경공단: 장소별 기본 소음 프로파일
[해당 환경의 소음 수준]
   ↓ RAED: 행동×환경 실제 수치
[현재 위치 + 행동]
   ↓ Spotify: 수치에 대응하는 음악 특성 기준
[상황에 맞는 음악 특성 도출] → [추천]
```

예시: studying은 RAED 기준 44.7dB·활동 2.5(Quiet~Moderate) → 음악 간섭 최소화 필요 → Spotify에서 instrumentalness 높고 energy 낮은 lofi(0.83)·classical(0.73)이 조건 충족 → 이 흐름이 LLM 프롬프트의 추천 로직 근거가 됨.

### 4-5. 추천 우선순위 설계
| 우선순위 | 변수 | 역할 |
|---|---|---|
| 1 | **소음 수준** | 음악의 가청성과 직결. 80dB 초과 시 목표 무관 소음 마스킹 최우선 |
| 2 | **사용자 목표** | 음악 분위기·장르 결정 |
| 3 | **위치** | 보조적 분위기 보정 |

`💡 어필 포인트` — 이 우선순위를 초기에 명확히 정의한 덕분에 이후 모든 LangGraph 노드 프롬프트가 일관된 방향을 가질 수 있었다. (회고에서 "설계 단계 결정이 가장 큰 영향"으로 꼽은 부분)

### 4-6. 추천엔진 진화 과정 (시행착오)
| 버전 | 방식 | 문제 | 결정 |
|---|---|---|---|
| v1.0 | Rule-based | 168개 조합(7×8×3) 룰 작성 복잡, 룰 안 곡만 나와 다양성 부족 | 폐기 |
| v1.5 | 단순 GPT 호출 | **할루시네이션** (존재하지 않는 곡/아티스트 생성) | 폐기 |
| v2.0 | **LangGraph + Spotify API** | — | 채택 |

v2.0 핵심: **역할 분리** — LLM은 상황 해석·장르 판단만, 실제 곡 검색은 Spotify API. 할루시네이션을 구조적으로 차단.

---

## 5. 핵심 분석 결과 (중요도 순)

### ① Deep Link Conversion (DLC) — 85.15% `가장 신뢰도 높은 지표`
- **결과**: 추천을 본 사용자 중 85.15%가 "Spotify에서 듣기" 클릭. `playlists_final.id` ↔ `spotify_link_click.playlist_id` Left Join으로 계산.
- **의미**: 설문·키워드 매칭보다 **실제 행동**을 직접 반영. 추천 수용도의 가장 솔직한 신호.
- **상황별 약점**: 카페-분노 0%, 도서관-위로 25%, 코워킹-미설정 40%, 도서관-휴식 50%, 카페-위로 50% → **공공 공간 × 개인적 감정 케어** 조합에서 급락. 장소 성격과 감정 상태가 상충할 때 추천 로직 약화.
- **부가 발견**: 지연 시간과 전환율 사이 뚜렷한 관계 없음 → 사용자는 속도보다 **추천의 적절성**을 중시.
- **비즈니스 활용**: 전환 동기가 약한 상황 조합을 우선 개선 대상으로 특정 가능.

### ② 사용자 경향성 — 맥락 기반 일관 사용 패턴
- 위치: 집·실내 > 코워킹 > 이동 중 (일상 공간 중심)
- 목표: 집중 / 휴식 / 활력 중심 (감정 조절보다 기능적 목적)
- 대표 조합(히트맵): 카페×집중·활력, 집/실내×휴식·수면, 코워킹×집중·활력, 헬스장×활력
- **의미**: 사용자가 무작위가 아닌 일관된 맥락으로 사용 → RAED로 도출한 추천 로직이 실제 사용 맥락과 정합함을 **간접 검증**.

### ③ 만족도 & 세그먼트
- 평균 평점 **3.27 / 4.0**, 재사용 의사 **93.3%**(18명 기준이라 1~2명 영향 큼 → 참고 지표로 해석)
- 재사용 Yes 그룹 평점 3.36 vs No 그룹 2.0 → 강한 양의 상관
- **충성파(평점≥3.0 & reuse=Yes) 15명** / **이탈위험파(평점≤2.0 & reuse=No) 2명(user 11, 14)**
- 충성파 평균 요청 23.3회 vs 이탈위험 6회 vs 기타 16회 → 만족이 사용 빈도와 직결
- 충성파 선호: energetic 70.4% / calm 29.6%, lyrics_on 88.9%

### ④ 이탈 원인 — 두 갈래
- **user 14 (1/6 이탈)**: 취향 미스매치 — "J팝 가수·인디 가수를 선호로 골랐는데 안 맞아요" → **추천 품질** 문제
- **user 11 (1/12 이탈)**: "플레이리스트 나오는 데 너무 오래 걸려요" → **시스템 성능(응답 속도)** 문제
- **의미**: 이탈은 단일 원인이 아니라 품질/성능 두 방향. 초기 몇 회 경험이 장기 사용을 가르는 분기점. 시스템 업데이트 시 일시적 성능 저하가 초기 이탈로 이어질 수 있음.

### ⑤ Agent Latency
- 평균 7.62초 / 중앙값 6.34초 / 최장 18.97초 / 최단 1.01초 (긴 꼬리 분포)
- 날짜-지연 상관계수 −0.2231(우하향) — 단순 우하향이 아니라 **안정(7초대) → 급등(개인화 도입 후 14초대) → 급락(최적화 후 3초대)** 흐름
- **교훈**: 신규 기능 도입 시 성능 영향 사전 검토 + 도입 직후 모니터링 체계 필요.

### ⑥ Satisfaction RMSE — 1.1353
- 지연 시간만으로 만족도를 예측(`Predicted = 4.0 − 0.15 × latency_sec`)했을 때 실제 별점과 평균 1점 이상 오차.
- **진짜 가치**: "지연 시간만으로는 만족도를 설명할 수 없다"를 수치로 증명 → 콘텐츠 품질 변수(Context Accuracy, 장르 일치도)를 예측 모델에 넣어야 한다는 방향성 도출.

### ⑦ Context-Sentiment Accuracy 81.17% / Persona-Genre Accuracy 100%
- Context: location·goal 키워드가 `recommendation_reason`에 포함되는지 평균. 19% 미달은 키워드 대신 유의어("집중"→"편안한/활기찬") 사용 → 키워드 매칭 한계, Semantic Similarity 필요.
- Persona 100%: 측정 기준이 관대(키워드 1회 등장=1점). "취향 정보 누락 여부 확인"이라는 목적엔 유효.

---

## 6. 프로젝트에서 직접 설계한 것 (차별화 요소)

### 6-1. KPI를 0에서 직접 정의
정답 레이블도 표준 지표(Precision/Recall/NDCG)도 쓸 수 없는 상황. "정확함"의 기준 자체를 데이터·서비스 맥락에서 직접 만들었다.

**원래 8개 → 최종 5개 (탈락 사유 기록)**

| 분류 | 지표 | 결과 |
|---|---|---|
| 엔진 구조로 이미 해결 | Search Hit Rate, Personalization Precision, Diversity | 엔진에 내재화되어 별도 측정 불필요 |
| Spotify API 제약 | Semantic Alignment / Audio Relevance Score → Smart Genre Relevance → 최종 Context-Sentiment Accuracy로 타협 | Audio Features API 403 차단, Track 단위 장르 미제공 |
| DB 설계/개발 우선순위 | Re-roll Rate(retry 세션 구분 불가), 저장 기반 Precision(기능 지연), 청취 지속시간(딥링크 구조상 측정 불가) | 측정 실패 |
| 미검증 | 규칙 커버리지(245조합) | LLM이라 다 커버될 거라 가정하고 미실시 |
| **최종 측정** | Context-Sentiment Accuracy(81.17%), Persona-Genre(100%), Agent Latency(7.62s), DLC(85.15%), Satisfaction RMSE(1.1353) | 측정 완료 |

### 6-2. 변수/지표 신규 설계
- 7개 장소 카테고리 + 5개 소음 구간(실제 분포 반영) 매핑 규칙
- 행동×환경×음악 특성을 잇는 추천 매트릭스 (소음 마스킹 / 간섭 최소화 / 상황 적응형 3전략)
- App Stay Time(`minutes_from_creation`), Re-entry Rate(딥링크 이후 추적 불가의 우회 대안) 등 구조적 한계를 우회하는 대체 지표 설계

### 6-3. 분석 환경 자체를 설계
- **사전배포를 직접 제안** (팀 원안에 없던 단계). "실사용자 로그 없이는 KPI 측정 불가"라는 논리로 설득.
- 수집할 **로그 스키마를 DA가 직접 정의**해 개발팀에 전달.

`💡 어필 포인트` — 측정 가능한 것과 중요한 것이 항상 일치하지 않음을 인지하고, **각 지표가 무엇을 측정하며 어디까지 신뢰할 수 있는지를 함께 서술**한 점. 수치를 곧이곧대로 받지 않는 분석 태도.

---

## 7. 기술적 성장

| 영역 | 구체 내용 |
|---|---|
| **SQL / 데이터 결합** | 복합 키 조인 설계 — `user_id, location, decibel, goal` 4컬럼 복합 키로 RMSE용 데이터 매칭(26개로 줄지만 정확도↑). 데이터가 적을수록 조인 설계가 결과에 크게 영향. |
| **Python (pandas)** | Left/Inner Join, 키워드 매칭 정확도 함수, 예측 만족도 함수, 세그먼트 조건 필터링, RMSE 계산(sklearn) |
| **통계 분석** | 평균/중앙값/표준편차·변동성 해석, 상관계수, 시계열 추세, RMSE, 세그먼트 비교, 전환율(히트맵) |
| **시각화** | 히트맵(location×goal), 막대/파이, latency 분포·추세, 전환율 히트맵, 시계열 라인 |
| **분석 방법론** | AARRR 퍼널, RFM(Monetary→몰입도 재해석), 코호트/세그먼트, 시계열, 최신성(Recency), A/B(`확인 필요`) |
| **추천 시스템/LLM** | Rule-based → GPT → LangGraph 9노드 설계, Spotify Search API 문법 리서치, LLM 역할 분리 설계 |

### LangGraph 9노드 아키텍처
```
analyze_preference → context_analysis → search_query_generator ←─┐
   → tools(병렬검색) → preference_search → selection               │ (재시도 ≤3)
   → remix_track_filter → quality_validator ── 실패 ──────────────┘
        └ 통과 → generate_reason → END
```
- **tools**: 8개 쿼리를 `ThreadPoolExecutor`로 병렬 검색, `seen_ids`로 중복 제거(최대 50 후보)
- **selection 기준**: 선호 아티스트 20%(2곡) / 한국 노래 50%(5곡) / 신곡 20%(2곡) / 인기도 4:4:2
- **quality_validator**: 기준 미달 시 `search_query_generator`로 회귀(최대 3회) — 무한 루프 방지
- **Spotify API 제약 대응**: Audio Features/Recommendations API가 신규 앱 차단(2024-11-26 공지)되어, `genre`/`year`/`artist`/`track` 텍스트 쿼리로 후보를 넓게 모은 뒤 LLM이 상황 적합성 판단하는 구조로 선회
- **부가 설계**: SEO 스팸 트랙 제외(`SPAM_KEYWORDS`), 리믹스/라이브 제거, popularity는 Search 응답값 그대로 사용

---

## 8. 소프트 스킬

- **협업**: 팀 내 유일 DA로서 분석 전 영역을 단독 정의. 수집할 로그 스키마를 개발팀과 협의(앱 종료 로그·저장 로그 등 구현 난이도 높은 항목은 제외 조율).
- **설득/제안**: 사전배포를 논리로 설득해 실행시킴 → 분석 환경 확보.
- **의사결정 → 실행 연결**: 이탈 원인 분석 결과를 팀에 공유 → 개인화 로직 도입 시 **응답 속도 모니터링 강화**로 개발 우선순위가 실제로 변경됨.
- **일정/제약 관리**: API 차단·기능 지연 등 외부 제약 속에서 측정 가능한 지표로 범위를 재조정.
- **자기 검증 태도**: 검토해줄 동료가 없는 환경에서 판단 기준을 스스로 세우고, 틀린 부분도 스스로 발견·기록(회고 6편).

`💡 어필 포인트` — "분석이 보고서로 끝나지 않고 실제 결정으로 이어진 경험"은 면접에서 강력한 임팩트 사례.

---

## 9. 어려웠던 점과 해결

| 유형 | 문제 | 대응 |
|---|---|---|
| 기술(외부 API) | Spotify Audio Features/Recommendations API 신규 앱 차단(403) | 오디오 특성 기반 정밀 필터링 포기 → 텍스트 쿼리 수집 + LLM 판단 구조로 선회. KPI는 키워드 매칭 기반 Context-Sentiment Accuracy로 타협 |
| LLM 구조 | GPT 할루시네이션(없는 곡 생성) | LLM은 판단, 검색은 Spotify API로 **역할 분리** → 원천 차단 |
| 데이터/DB | retry 세션 구분 불가 | Re-roll Rate 측정 포기, DB 설계 단계에 분석 관점을 미리 반영했어야 한다는 교훈 |
| 데이터 결합 | user_id 단일 조인 시 어떤 세션 리뷰인지 특정 불가 | 4컬럼 복합 키로 정확도 확보(샘플 26개) |
| 규칙 설계 | location 매핑에서 Crowded Place 미처리 → 의도치 않은 fallback | 예외 케이스를 먼저 정의하고 시작해야 한다는 교훈 |
| 표본 | 18명, 이탈 2명 — 통계적 유의성 부족 | 패턴은 방향 힌트로만 해석, 표본 확보를 다음 과제로 명시 |

---

## 10. 실패와 시행착오

- **실패한 추천 방식**: Rule-based v1.0(다양성 부족), 단순 GPT(할루시네이션) 모두 폐기.
- **폐기한 KPI 설계**: Audio Relevance Score → Smart Genre Relevance Score(아티스트 장르가 곡 장르를 100% 대변 못 해 fail 다발) → 하이브리드도 "억지로 끼워맞추는 느낌"이라 폐기. 최종 단순 키워드 매칭으로 착지(찜찜함 남김).
- **변경된 방향성**: "Spotify에서 실제 데이터를 가져와야 객관적이다"라는 생각에 갇혀 있었으나, 돌아보면 **EDA에서 정의한 상황별 energy/valence/tempo 범위가 이미 충분한 ground truth**였음. API 없이도 됐다.
- **놓친 데이터**: DLC 초기 1주차 클릭 로그 미기록 → 초기 전환율 손실. 로그 설계 체크리스트를 배포 전에 DA가 직접 만들었어야 함.
- **버전 관리 부재**: 이전 버전 코드/성능 기록 소실 → 각 단계 실제 성능 수치를 남기지 못함. 다음엔 버전 태그 + 성능 기록 파일 저장.

---

## 11. 가장 의미 있었던 순간

1. **사전배포 제안이 받아들여진 순간** — "분석 가능한 환경을 먼저 만들어야 한다"는 논리가 통해, 추측이 아닌 실제 데이터로 시스템을 평가할 수 있게 됨. DA의 역할이 "주어진 데이터 분석"을 넘어 "분석 환경 설계"까지 포함한다는 걸 체감.
2. **3개 데이터셋을 연결해 임계값을 만든 순간** — "studying엔 lofi/classical"을 감이 아닌 수치 근거로 만들었을 때.
3. **분석이 개발 우선순위를 바꾼 순간** — 이탈 원인 분석이 응답속도 모니터링 강화로 이어진 경험.

---

## 12. 면접 활용 포인트 (STAR)

### STAR ① — 환경을 만든 문제 해결 (사전배포 제안)
- **S**: 팀 원안에 사전배포가 없어 KPI를 측정할 실사용자 로그가 전무.
- **T**: 시스템이 잘 작동하는지 검증할 방법 확보.
- **A**: "로그 없이는 KPI 측정 불가" 논리로 사전배포를 제안·설득하고, 수집 로그 스키마를 직접 정의해 개발팀에 전달.
- **R**: 18명·약 300건 로그 확보 → DLC 등 5개 KPI를 실데이터로 측정.

### STAR ② — 협업/의사결정 (분석→우선순위 변경)
- **S**: 개인화 기능 도입 후 일부 사용자 이탈 발생.
- **T**: 이탈 원인 규명.
- **A**: 이탈 2건을 개별 분석 → 취향 미스매치(user 14) vs 응답 지연(user 11) 두 갈래로 분리, 시계열로 도입 시점과 연결.
- **R**: 팀이 개인화 도입 시 응답속도 모니터링 강화로 개발 우선순위 변경.

### STAR ③ — 기술적 문제 해결 (API 제약 우회)
- **S**: Spotify Audio Features API가 신규 앱에 차단(403).
- **T**: 오디오 특성 기반 추천/검증을 다른 방식으로 달성.
- **A**: 텍스트 기반 쿼리로 후보 수집 + LLM 상황 판단 구조로 재설계. KPI도 측정 가능한 키워드 매칭 기반으로 재정의하되 한계를 명시.
- **R**: 할루시네이션 차단 + 동작하는 엔진/측정 체계 확보. (교훈: EDA 결과가 이미 ground truth였다는 회고)

### STAR ④ — 데이터 결합 정확도
- **S**: 한 사용자의 여러 세션 중 어떤 리뷰인지 단일 키로 특정 불가.
- **A**: 4컬럼 복합 키로 매칭(26건). 데이터가 적을수록 조인 설계가 결과에 미치는 영향이 크다는 걸 수치로 확인.

> 갈등 해결/리더십 사례: 명시적 갈등 기록은 없음(`확인 필요`). "팀 내 유일 DA로서 판단 기준을 스스로 세운 경험"을 리더십 인접 사례로 활용 가능.

---

## 13. 포트폴리오 활용 포인트

- **가장 강조할 경험**: ① 분석 환경(사전배포·로그 스키마) 직접 설계 ② 자체 KPI 0→설계 ③ 분석→의사결정 연결.
- **차별화 포인트**: 정답 레이블 없는 LLM 추천에서 "정확함"의 기준을 직접 정의했고, 측정 한계까지 솔직히 서술하는 분석 태도.
- **핵심 성과(수치)**: DLC 85.15%, Context Accuracy 81.17%, 평균 평점 3.27/4.0, 재사용 93.3%, Latency 평균 7.62초.
- **대표 시각화 후보**:
  1. Location × Goal 히트맵 (사용 패턴 + 전환율 두 버전)
  2. Latency 시계열(안정→급등→급락) — 기능 도입 영향 스토리
  3. 충성파 vs 이탈위험파 빈도/시계열 비교
  4. 3개 데이터셋 연결 다이어그램(데이터→추천 임계값)
  5. KPI 8→5 탈락 사유 정리표 (의사결정 능력 어필)

---

## 14. 회고

### 잘한 점
- 분석 가능한 환경(사전배포)을 직접 만들었다.
- 3개 데이터셋을 연결해 추천 로직을 수치 근거로 만들었다.
- 표준 지표가 없는 상황에서 서비스 맥락 KPI를 직접 정의했다.
- 분석 결과를 팀 의사결정으로 연결했다.

### 아쉬운 점
- 표본이 너무 작았다(18명).
- 설계한 KPI 중 절반 이상을 실행하지 못했다(API·DB·우선순위 제약).
- DLC 초기 데이터를 놓쳤다.
- location 매핑이 불완전했다(예외 미정의).
- 모델 자체 성능 평가와 서비스 KPI를 처음부터 구분하지 못했다.
- 버전 관리 부재로 이전 코드/성능 기록이 소실됐다.

### 다시 한다면
- 사전배포 단계에서 표본 수 확보를 의식적으로 챙긴다.
- 측정 가능한 로그를 개발팀과 배포 전에 체크리스트로 확정한다.
- Audio Relevance Score를 EDA 기준값으로 직접 구현한다(API 의존 X).
- 추천 결과 화면에서 즉시 피드백(좋아요/싫어요)을 받는 구조 + 개별 곡 좋아요로 세밀한 품질 평가.
- App Stay Time·Re-entry Rate 등 측정 가능했던 보완 지표까지 실제 분석.
- 콘텐츠 기반 → 행동 기반 → 하이브리드 순으로 추천 시스템을 재설계.

---

## 15. 최종 요약

**1문장**
> 정답 레이블도 표준 지표도 없는 LLM 음악 추천 서비스에서, 분석 환경(사전배포)과 평가 기준(자체 KPI)을 직접 설계해 18명의 실사용자 로그로 시스템을 검증하고 개선 방향을 도출한 프로젝트.

**3문장**
> DIP는 위치·목표·소음을 입력받아 LLM이 상황에 맞는 음악을 추천하는 서비스로, 팀 내 유일한 DA로서 분석의 방향·범위·방법론을 전부 정의했다. 환경공단·RAED·Spotify 3개 데이터셋을 연결해 추천 로직의 임계값을 수치로 만들고, 정답이 없는 상황에서 DLC·Context Accuracy 등 5개 KPI를 직접 설계해 측정했다. 특히 팀 원안에 없던 사전배포를 제안·실행해 실데이터를 확보하고, 이탈 원인 분석을 개발 우선순위 변경으로 연결했다.

**1분 자기소개 버전**
> DIP는 사용자의 위치·활동 목표·주변 소음을 입력받아 LLM이 상황에 맞는 플레이리스트를 추천하는 서비스였고, 저는 팀에서 유일한 데이터 분석가로 참여했습니다. LLM 추천에는 정답 레이블이 없어서 "정확하다"는 게 무엇인지부터 정의해야 했습니다. 그래서 환경공단 소음 데이터, 행동·환경 데이터(RAED), Spotify 오디오 특성 데이터 세 가지를 연결해 "어떤 소음·목표엔 어떤 음악 특성이 맞는지"를 수치 근거로 만들었습니다. 더 중요한 건, 시스템을 평가할 실사용자 로그가 없다는 걸 깨닫고 팀 원안에 없던 사전배포를 직접 제안해 18명·약 300건의 로그를 확보한 것입니다. 이 데이터로 딥링크 전환율 85%, 상황 정확도 81% 같은 자체 KPI를 측정했고, 카페-분노처럼 공간과 감정이 상충하는 조합에서 전환율이 0%로 떨어진다는 약점, 그리고 이탈이 취향 미스매치와 응답 지연 두 갈래라는 점을 찾아냈습니다. 이 분석은 보고서로 끝나지 않고, 개인화 도입 시 응답속도 모니터링을 강화하는 방향으로 팀의 개발 우선순위를 바꾸는 결정으로 이어졌습니다. 이 프로젝트에서 저는 분석가의 역할이 주어진 데이터를 분석하는 것을 넘어, 분석할 환경과 기준 자체를 설계하는 것까지 포함한다는 걸 배웠습니다.

---

### 부록 — 확인 필요 항목
- 전체 프로젝트 시작일(사전배포 외)
- 환경공단/Spotify 데이터셋 정확한 행 수
- A/B 테스트 수행 여부 및 내용 (메모리상 언급 있으나 첨부 문서 미확인)
- 명시적 팀 내 갈등 해결 사례
- `TAVE_리듬타고가는중_DIP_후반기결과물.pdf`(46p, 이미지 슬라이드) 내 추가 수치/시각화 — 텍스트 추출 불가로 미반영
